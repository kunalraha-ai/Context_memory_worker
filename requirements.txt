# ─────────────────────────────────────────────────────────────────────────────
# Aden Hive — Context Memory Worker  •  Python dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Runtime requirements
#   • Python  >= 3.10
#   • Ollama  running locally  →  https://ollama.com/download
#     Pull required model:  ollama pull nomic-embed-text
#
# ── INSTALLATION (all platforms, including Windows — no C++ needed) ──────────
#
#   pip install -r requirements.txt
#
# ─────────────────────────────────────────────────────────────────────────────

# Vector database — local persistent, pure-Python wheels on all platforms
lancedb>=0.6.0,<1.0.0

# Arrow columnar backend (pulled by lancedb, pinned for reproducibility)
pyarrow>=14.0.0,<20.0.0

# HTTP client — Ollama API calls
requests>=2.31.0,<3.0.0